### 📘 API Endpoints (Quiz App)

- `GET /api/quizzes` – Get all quizzes
- `GET /api/quizzes/{id}` – Get quiz by ID
- `POST /api/quizzes` – Create a new quiz
- `DELETE /api/quizzes/{id}` – Delete quiz
- `POST /api/submit` – Submit answers (returns score)
